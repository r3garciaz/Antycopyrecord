---

---

<section
    class="relative min-h-screen flex items-center justify-center overflow-hidden pt-20"
>
    <!-- Background Effects -->
    <div class="absolute inset-0 z-0">
        <!-- Banner Image -->
        <img
            src="/banner.jpg"
            alt="Studio Banner"
            class="absolute inset-0 w-full h-full object-cover opacity-40 mix-blend-overlay"
        />
        <div
            class="absolute inset-0 bg-linear-to-b from-brand-bg/90 via-brand-bg/50 to-brand-bg"
        >
        </div>

        <div
            class="absolute top-1/4 left-1/4 w-96 h-96 bg-brand-primary/10 rounded-full blur-3xl animate-pulse"
        >
        </div>
        <div
            class="absolute bottom-1/4 right-1/4 w-[500px] h-[500px] bg-purple-900/10 rounded-full blur-3xl"
        >
        </div>
        <div
            class="absolute inset-0 bg-[url('/noise.png')] opacity-5 mix-blend-overlay"
        >
        </div>
    </div>

    <div class="relative z-10 max-w-7xl mx-auto px-4 text-center">
        <!-- Badge -->
        <div
            class="inline-block mb-6 animate-fade-in"
            style="animation-delay: 0.1s;"
        >
            <span
                class="px-4 py-2 border border-brand-primary/30 rounded-full bg-brand-primary/10 text-brand-primary text-xs tracking-[0.2em] font-bold uppercase backdrop-blur-sm"
            >
                Experiencia de Sonido Premium
            </span>
        </div>

        <!-- Headline -->
        <h1
            class="text-5xl md:text-7xl lg:text-8xl font-display font-bold text-white mb-8 leading-tight animate-fade-in"
            style="animation-delay: 0.2s;"
        >
            SINFONÍA DESDE EL <br />
            <span
                class="text-transparent bg-clip-text bg-linear-to-r from-brand-primary to-yellow-200"
                >UNDERGROUND</span
            >
        </h1>

        <!-- Subheadline -->
        <p
            class="text-brand-muted text-lg md:text-xl max-w-2xl mx-auto mb-10 font-light tracking-wide animate-fade-in"
            style="animation-delay: 0.3s;"
        >
            La calidad que exigen las grandes ligas, con la esencia real del
            underground. Producción completa, beats duros y videoclips con
            identidad.
        </p>

        <!-- CTAs -->
        <div
            class="flex flex-col md:flex-row items-center justify-center gap-4 animate-fade-in"
            style="animation-delay: 0.4s;"
        >
            <a
                href="#contact"
                class="w-full md:w-auto px-8 py-4 bg-brand-primary text-brand-bg font-bold text-lg rounded-full hover:bg-white transition-all duration-300 transform hover:scale-105 shadow-[0_0_20px_rgba(212,175,55,0.3)]"
            >
                Comienza tu Proyecto
            </a>
            <a
                href="#portfolio"
                class="w-full md:w-auto px-8 py-4 border border-brand-secondary bg-brand-secondary/50 text-white font-medium text-lg rounded-full hover:border-brand-primary hover:text-brand-primary transition-all duration-300"
            >
                Escuchar Muestras
            </a>
        </div>
    </div>

    <!-- Scroll Indicator -->
    <div
        class="absolute bottom-10 left-1/2 transform -translate-x-1/2 animate-bounce opacity-50"
    >
        <svg
            xmlns="http://www.w3.org/2000/svg"
            fill="none"
            viewBox="0 0 24 24"
            stroke-width="1.5"
            stroke="currentColor"
            class="w-6 h-6 text-brand-primary"
        >
            <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M19.5 8.25l-7.5 7.5-7.5-7.5"></path>
        </svg>
    </div>
</section>
